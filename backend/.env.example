# PesacodeDB Backend Environment Configuration
# Copy this file to .env and update the values

# ========================================
# Database Connection
# ========================================
# PesaDB Connection URL (custom RDBMS)
# Format: pesadb://localhost/database_name
# The database will be automatically created if it doesn't exist
PESADB_URL=pesadb://localhost/default

# Optional: Specify custom data directory in the URL
# PESADB_URL=pesadb://localhost/myapp?data_dir=/path/to/data

# ========================================
# API Security
# ========================================
# CRITICAL: Generate a strong, unique API key for production!
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=your_secure_api_key_here

# API Key Requirement
# Set to "false" for local development (no API key required)
# Set to "true" for production (API key required for all requests)
REQUIRE_API_KEY=true

# ========================================
# CORS Configuration
# ========================================
# For development: "*" (allow all origins)
# For production: "https://yourdomain.com,https://app.yourdomain.com"
CORS_ORIGINS="*"

# ========================================
# Server Configuration
# ========================================
# Debug Mode
# Set to "true" to see detailed error messages (development only)
# Set to "false" in production to hide internal error details
DEBUG=false

# Server host and port (optional - defaults shown)
# PORT=8000
# HOST=0.0.0.0

# ========================================
# AI Features (Optional)
# ========================================
# Gemini AI API Key (for AI assistant features)
# Get your key from: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here
