# PesacodeDB Backend Configuration
# Copy this file to .env and fill in your values

# === Required Configuration ===

# API Key for securing your API endpoints
# Generate a strong random key (e.g., using: openssl rand -hex 32)
API_KEY=your_secure_api_key_here

# Whether to require API key authentication (true/false)
# Set to "false" only for local development
REQUIRE_API_KEY=true

# === Database Configuration ===

# PesaDB connection URL
# Format: pesadb://localhost/database_name?data_dir=/path/to/data
# For Railway, you might want to use a volume path like: pesadb://localhost/default?data_dir=/app/data
PESADB_URL=pesadb://localhost/default

# === AI Configuration (Optional) ===

# Google Gemini API Key for AI features
# Get your key from: https://makersuite.google.com/app/apikey
# Leave empty to disable AI features
GEMINI_API_KEY=

# Gemini model to use
GEMINI_MODEL=gemini-1.5-flash-latest

# === CORS Configuration ===

# Allowed CORS origins (comma-separated)
# For production, specify your frontend URL
# For development, use * to allow all origins
CORS_ORIGINS=*

# === Optional Configuration ===

# Enable debug mode (shows detailed error messages)
DEBUG=false
