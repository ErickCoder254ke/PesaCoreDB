# ==============================================================================
# CLIENT APPLICATION ENVIRONMENT CONFIGURATION
# Template for applications connecting to PesacodeDB
# ==============================================================================

# ------------------------------------------------------------------------------
# DATABASE CONNECTION
# ------------------------------------------------------------------------------
# PesacodeDB connection URL
# Format: pesadb://localhost/database_name[?data_dir=/path/to/data]
#
# Examples:
#   pesadb://localhost/chamaa              (uses default data/ directory)
#   pesadb://localhost/myapp?data_dir=/var/pesadb
#
PESADB_URL=pesadb://localhost/chamaa

# Database name (should match the database in PESADB_URL)
DB_NAME=chamaa

# ------------------------------------------------------------------------------
# APPLICATION SECURITY
# ------------------------------------------------------------------------------
# Secret key for your application (Flask/Django session encryption, JWT, etc.)
# Generate a new key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=fee66ad09492f2a81b631bc77dfbe092

# ------------------------------------------------------------------------------
# BACKEND API CONFIGURATION (if using the REST API)
# ------------------------------------------------------------------------------
# Backend API base URL (where server.py is running)
BACKEND_API_URL=http://localhost:8000/api

# API key for authenticating with the backend
# Must match the API_KEY in backend/.env
API_KEY=pesacodedb_dev_key_change_in_production_2024

# ------------------------------------------------------------------------------
# OPTIONAL: DIRECT DATABASE CONNECTION (Python apps)
# ------------------------------------------------------------------------------
# If your app directly imports rdbms modules:
# from rdbms.connection import connect
# conn = connect(os.getenv("PESADB_URL"))

# Data directory (optional - defaults to 'data' in project root)
# PESADB_DATA_DIR=/path/to/database/files

# ------------------------------------------------------------------------------
# APPLICATION SETTINGS
# ------------------------------------------------------------------------------
# Flask/Django debug mode
DEBUG=True

# Application environment
ENVIRONMENT=development

# CORS origins (if your app serves an API)
CORS_ORIGINS=*

# ------------------------------------------------------------------------------
# NOTES
# ------------------------------------------------------------------------------
# 1. The database will be auto-created on first connection if it doesn't exist
# 2. Make sure PESADB_URL and DB_NAME reference the same database name
# 3. For production, set DEBUG=False and use strong SECRET_KEY
# 4. Keep this file out of version control (.gitignore)
